<style>
  .roles-page {
    min-height: 100vh;
    background-color: #f8f9fa;
  }

  .page-header {
    text-align: center;
    padding: 6rem 2rem;
    background-color: #fff;
    background-image: url('/Wood.jpg');
    background-size: cover;
    background-position: center;
    position: relative;
    color: white;
  }

  .page-header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
  }

  .page-header h1, .page-header p {
    position: relative;
    z-index: 1;
  }

  .page-header h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .page-header p {
    font-size: 1.5rem;
    opacity: 0.9;
  }

  .roles-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 4rem 2rem;
  }

  .roles-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .role {
    background: white;
    padding: 2rem;
    border-radius: 1rem;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    display: flex;
    flex-direction: column;
  }

  .explorer { border-top: 5px solid #00b894; }
  .supporter { border-top: 5px solid #0984e3; }

  h2 {
    font-size: 2rem;
    margin-bottom: 1rem;
    color: #000000;
  }

  .description {
    font-size: 1.1rem;
    color: #636e72;
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .features {
    list-style: none;
    padding: 0;
    margin-bottom: 2rem;
    flex-grow: 1;
  }

  .features li {
    margin: 0.8rem 0;
    padding-left: 1.5rem;
    position: relative;
  }

  .features li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: #00b894;
  }

  .cta-button {
    display: inline-block;
    background-color: #00b894;
    color: white;
    padding: 1rem 2rem;
    border-radius: 2rem;
    text-decoration: none;
    text-align: center;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .cta-button:hover {
    background-color: #00a28d;
    transform: translateY(-2px);
  }

  @media (max-width: 768px) {
    .page-header {
      padding: 4rem 1rem;
    }

    .page-header h1 {
      font-size: 2rem;
    }

    .roles-content {
      padding: 2rem 1rem;
    }
  }
</style>