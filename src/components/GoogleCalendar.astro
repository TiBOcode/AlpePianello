---
// GoogleCalendar.astro
---

<div class="calendar-wrapper">
  <div class="calendar-container">
    <iframe 
      src="https://calendar.google.com/calendar/embed?src=mjdihdrdnejovh75r684nj0nt4@group.calendar.google.com&showTitle=0&showNav=1&showPrint=0&showTabs=1&showCalendars=0&showTz=0" 
      frameborder="0" 
      scrolling="no"
      loading="lazy">
      <p>Loading calendar...</p>
    </iframe>
  </div>
</div>

<style>
  .calendar-wrapper {
    width: 100%;
    max-width: 1200px;
    margin: 2rem auto;
    padding: 0 1rem;
  }

  .calendar-container {
    position: relative;
    width: 100%;
    /* 16:9 aspect ratio for the container */
    padding-bottom: 56.25%;
    height: 0;
    overflow: hidden;
    background: #f5f5f5;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }

  iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: none;
    border-radius: 12px;
  }

  /* Loading state */
  iframe:not([loaded]) + p {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 1.1rem;
    color: #666;
  }

  @media (max-width: 768px) {
    .calendar-wrapper {
      margin: 1rem auto;
    }
    
    .calendar-container {
      /* Taller aspect ratio for mobile */
      padding-bottom: 75%;
    }
  }
</style>